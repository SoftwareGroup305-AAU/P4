setup
{
    float ni = (2 * 3) + (1 * (2 + 1));

    ni == 9 ? ni = 9 : ni = 8;

    int i = 0;
    i = i + 1;

    while (i < 10)
    {
        i++;
    }

    bool yesyes = false;
    for (int j = 10; j > 0; j--)
    {
        if (!yesyes)
        {
            break;
        }
        if (yesyes && yesyes)
        {
            continue;
        }
    }
    i += 2;
}
update
{
    �nt x = 1;
    int y = 2;
    int z = x + y;
}
