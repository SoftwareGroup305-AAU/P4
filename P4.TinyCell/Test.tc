setup
{
    float ni = (2 * 3) + (1 * (2 + 1));

    ni == 9 ? ni = 9 : ni = 8;

    int i = 0;
    i = i + 1;

    while (i < 10)
    {
        i++;
    }

    bool yesyes = false;
    for (int j = 10; j > 0; j--)
    {
        if (!yesyes)
        {
            break;
        }
        if (yesyes && yesyes)
        {
            continue;
        }
    }
    i += 2;
}
update
{
    pin tester = 1;
    tester = 2;
    set tester to high;

    char nunununu = "c";

    testfunction(tester);

    int varvarvar = testfunction(tester);
}
int testfunction(pin par)
{
    bool tttt = false;
    int number;
    if (tttt != true)
    {
        number = 1;
    }
    else
    {
        number = 0;
    }
    return number;
}